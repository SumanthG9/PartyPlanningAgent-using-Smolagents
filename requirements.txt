# Pruned requirements: keep only packages explicitly imported in the codebase
smolagents==1.24.0
numpy==2.4.2
python-dotenv==1.2.1
langfuse==3.12.1
openinference-instrumentation-smolagents==0.1.21
# Note: transitive dependencies will be resolved by pip during install

